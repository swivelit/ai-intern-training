# Handwritten Digit Generation using GAN (MNIST)

## Objective
Generate realistic handwritten digits using a simple GAN trained on MNIST.

## How to Run
```bash
pip install -r requirements.txt
cd src
python train.py
