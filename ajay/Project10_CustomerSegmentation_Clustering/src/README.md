@"
# Project 10: Customer Segmentation using Clustering

## Overview
This project performs customer segmentation on the Mall Customers dataset using:
- K-Means
- Hierarchical (Agglomerative)
- DBSCAN

It generates cluster visualizations and cluster profile summaries in the `outputs/` folder.

## Setup
```bash
python -m pip install -r requirements.txt
