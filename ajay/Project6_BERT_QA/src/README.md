# Project 6 — Question Answering using BERT (SQuAD v1.1)

This project fine-tunes a Transformer QA model on the SQuAD v1.1 dataset and provides a CLI script to run question answering from the command line.

## Setup

```bash
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
