# Email Spam Classifier (SpamBase Dataset)

This project builds a binary classifier using Logistic Regression, Naive Bayes, SVM, and kNN.

## How to Run

```
pip install -r requirements.txt
python main.py
```

Dataset included in /data folder.
