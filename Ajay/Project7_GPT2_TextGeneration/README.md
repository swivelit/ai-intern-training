# Project 7 — GPT-2 Text Generation (Ajay)

Fine-tune GPT-2 (small) on a custom corpus and generate text using a CLI script.

## Setup

```bash
python -m venv .venv
# Windows PowerShell:
.venv\Scripts\activate
pip install -r requirements.txt
